CURSOR PROMPT — AUTH/RBAC #003/8
USER MODULE + PASSWORD / ACCOUNT LIFECYCLE FOUNDATION

1. Context
----------
We are building a long-term parish catechism backend platform.

Completed:
- Backend Foundation: COMPLETE
- Backend Bitbucket CI Foundation: COMPLETE
- AUTH/RBAC #001 — Modular Rules + Auth/RBAC Audit: COMPLETE
- AUTH/RBAC #002 — Schema + Entities + Migrations: COMPLETE
- AUTH/RBAC #002B — Schema Boundary Hardening: COMPLETE

Current:
- AUTH/RBAC #003 / 8 — User Module + Password/Account Lifecycle Foundation

Remaining:
- #004 Authentication + access token
- #005 Refresh token / session architecture
- #006 Role + Permission + RBAC guards
- #007 Auth/RBAC integration tests + security hardening
- #008 Final Auth/RBAC audit + module-boundary review

This prompt implements UserModule application/service behavior only.

DO NOT implement login controller/JWT/refresh/session/RBAC guards yet.

2. Required Reading
-------------------
Before changing anything:

1. Read PROJECT_RULES.md
   Pay special attention to:
   - modular architecture / microservice extraction
   - security
   - privacy
   - testing
   - database ownership rules
2. Read AGENTS.md
3. Read all .cursor/rules/*.mdc
4. Read:
   docs/AUTH_001_MODULAR_RULES_AND_AUTH_RBAC_AUDIT_REPORT.md
5. Read:
   docs/AUTH_002_AUTH_FOUNDATION_SCHEMA_REPORT.md
6. Read:
   docs/AUTH_002B_SCHEMA_BOUNDARY_HARDENING_REPORT.md
7. Inspect:
   src/modules/users/
   src/modules/auth/
   src/modules/access-control/
   src/database/
   package.json
   test/
8. Inspect current Git status using read-only commands.

3. Preflight — Migration State
------------------------------
AUTH #002B introduced corrective migration:
RemoveAuthUuidDatabaseDefaults1788055300000

Before service implementation:

Run:
npm run migration:show

If dev DB still has migration #2 pending:
run:
npm run migration:run

Do NOT modify migrations unless a real new issue is discovered.

Do NOT drop dev DB.

4. Primary Objective
--------------------
Implement the UserModule application/service foundation.

Required capabilities:

- email normalization
- account creation service
- secure password hashing
- credential verification
- account status checks
- narrow public contract for AuthModule
- repository access private inside UserModule
- comprehensive unit/integration tests

Do NOT implement HTTP endpoints yet.

5. Module Ownership
-------------------
UserModule owns:

- users table
- password_hash
- email normalization
- account lifecycle state
- credential verification

AuthModule must later authenticate through a public UserModule service.

AuthModule must NOT:
- inject User repository
- import UserEntity
- access password_hash directly

6. Password Hashing Algorithm
-----------------------------
AUTH #001 selected:

Argon2id

This prompt must validate and implement it.

Preferred package:
argon2

Before finalizing:
- install exact compatible dependency
- verify Node 22.23.1 compatibility
- verify native module works locally
- verify production Docker image builds
- verify CI-compatible Linux environment

If `argon2` fails in supported runtime/build environment:
- document concrete failure
- use bcrypt only as fallback
- do not silently switch

Preferred outcome:
Argon2id.

7. Password Hasher Boundary
---------------------------
Do NOT scatter direct `argon2.hash()` / `argon2.verify()` calls across services.

Create a small UserModule-owned abstraction.

Example concept:

PasswordHasher
or
PasswordHashService

Responsibilities:
- hash plaintext password
- verify plaintext vs stored hash

Keep implementation local to UserModule unless future reuse is genuinely needed.

Do NOT move it to generic `common/`.

8. Argon2 Configuration
-----------------------
Use Argon2id explicitly.

Choose safe server-side defaults.

Do NOT invent extreme resource parameters.

Use package defaults where they already represent secure Argon2id behavior unless project requirements justify explicit parameters.

If explicit parameters are used:
document:
- type
- memory cost
- time cost
- parallelism

Do NOT make these env-configurable yet unless there is a clear operational need.

9. Password Rules
-----------------
This prompt needs a minimal password policy for account creation.

Do not over-engineer.

Recommended:
- minimum length 12
- maximum reasonable bound, e.g. 128 or 256
- no arbitrary complexity requirements such as mandatory symbol/uppercase unless justified

The actual login DTO does not exist yet.

Implement validation at service/application boundary.

If DTOs are not required because there is no controller:
use typed command/input interfaces.

Never log plaintext passwords.

10. Email Normalization
-----------------------
Phase-1 login identifier:
email only.

Create a single normalization function/service.

Required:
- trim
- lowercase

Do NOT:
- remove dots
- normalize Gmail aliases
- rewrite domain semantics

Return normalized email consistently for:
- create account
- lookup
- credential verification

11. Email Validation
--------------------
Account creation must reject clearly invalid email shape.

Prefer using an existing validation mechanism/library already in project if suitable.

Do not introduce another large validation package solely for email.

Keep service-layer validation deterministic.

12. Account Creation Service
----------------------------
Implement UserModule service capability conceptually like:

createAccount({
  email,
  password,
  status?
})

Required:
- normalize email
- validate email
- validate password policy
- default account status appropriately
- hash password
- application-generated UUID v4 already handled by entity
- save UserEntity
- return SAFE account representation

Do NOT return:
- passwordHash

13. Default Account Status
--------------------------
Choose intentional default.

Preferred:
ACTIVE

for internal/admin-provisioned phase-1 accounts.

Reason:
there is no registration/email-verification workflow yet.

Document that future self-registration may use PENDING/verification flow separately.

Do not add PENDING enum now unless needed.

14. Safe User Account Contract
------------------------------
Create module-local/public interface or DTO-like read model for consumers.

Example:
UserAccountSnapshot

Possible fields:
- id
- email
- status
- createdAt
- updatedAt

Never include:
- passwordHash

Do not expose UserEntity as public service result.

15. Authentication Credential Boundary
--------------------------------------
Implement a public method for future AuthModule use.

Preferred shape conceptually:

verifyCredentials(email: string, password: string)

Return something like:

success:
{
  valid: true,
  account: {
    id,
    email,
    status
  }
}

failure:
{
  valid: false
}

or equivalent typed result.

Requirements:
- caller never receives passwordHash
- generic failure semantics
- email normalized internally
- password hash comparison performed inside UserModule

16. Account Status Enforcement
------------------------------
Credential verification must reject accounts that cannot authenticate.

At minimum:
ACTIVE -> eligible
INACTIVE -> reject
LOCKED -> reject

Do not reveal to caller whether failure was:
- wrong password
- inactive
- locked
- unknown email

Use generic failure result.

Detailed admin diagnostics belong elsewhere.

17. Timing / Enumeration Safety
-------------------------------
Avoid obvious email enumeration behavior.

Audit strategy for nonexistent user.

Preferred:
perform a dummy password hash verify or equivalent cost-balanced strategy where practical.

But do not create overly complex crypto machinery.

At minimum:
- public AuthModule result must remain generic
- no different error messages

Document any timing limitation honestly.

18. Repository Access
---------------------
Use TypeORM repository privately inside UsersService/UserAccountService.

Within UsersModule:

TypeOrmModule.forFeature([UserEntity])

Service may inject:
Repository<UserEntity>

But repository must NOT be exported.

Only public application service contract may be exported.

19. Service Naming
------------------
Choose clear naming.

Preferred:
UserAccountService

or:
UsersService

Avoid multiple overlapping services.

If one service is sufficient:
use one.

Do not create:
- UserRepositoryService
- UserManager
- AccountFacade
all at once.

20. UserModule Exports
----------------------
After this prompt, UsersModule may export:

- UserAccountService (or chosen public service)

It must NOT export:
- TypeOrmModule
- Repository
- UserEntity
- password hashing implementation unless intentionally public

21. No AuthModule Dependency
----------------------------
UsersModule must NOT import AuthModule.

Dependency direction remains:

AuthModule -> UsersModule

not:
UsersModule -> AuthModule

No cycle.

22. Duplicate Email Handling
----------------------------
Creation must handle duplicate normalized email.

Do not leak raw MSSQL error to callers.

At service layer:
- either pre-check + handle race with DB unique error
- or rely on unique DB constraint and map duplicate error

Best design should handle race safely.

Define a module-specific domain/application error such as:
UserEmailAlreadyExistsError

or equivalent.

Do not expose database error codes outside module.

23. Error Design
----------------
Since there is no controller yet, use module-level errors.

Examples conceptually:
- InvalidEmailError
- InvalidPasswordError
- UserEmailAlreadyExistsError

Do not create dozens of tiny error classes unnecessarily.

Consider:
- one validation result / exception structure
- one duplicate email error

Keep future controller mapping possible.

24. No User CRUD API
--------------------
Do NOT implement:
- GET /users
- POST /users
- PATCH /users
- DELETE /users

There is no authorization layer yet.

Account creation can exist only as service functionality + tests.

25. No Registration Endpoint
----------------------------
Do NOT implement public registration.

Future admin/provisioning flow will use UserModule service after RBAC exists.

26. No Login Endpoint
---------------------
Do NOT implement:
POST /auth/login

That is AUTH #004.

27. No JWT
----------
Do NOT add:
- @nestjs/jwt
- passport
- passport-jwt
- JWT config
- JWT secrets

That is AUTH #004.

28. No Session Logic
--------------------
Do NOT implement:
- auth_sessions service
- refresh token
- logout
- token family
- rotation

That is AUTH #005.

29. No RBAC Logic
-----------------
Do NOT implement:
- AccessControlService
- PermissionGuard
- RequirePermissions
- role assignments

That is AUTH #006.

30. Password Hash Rehash Strategy
---------------------------------
Audit whether Argon2 library supports detecting parameters requiring rehash.

If straightforward:
prepare design for future `needsRehash`.

Do not add automatic rehash complexity unless clean.

Document whether deferred.

31. Password Hash Column
------------------------
Confirm existing:
password_hash nvarchar(255)

fits Argon2 PHC string.

Add test if useful.

No schema migration should be required unless actual Argon2 output cannot fit.

32. Integration Tests
---------------------
Add DB integration tests for UserModule.

At minimum:

A. Create account
- persists normalized email
- passwordHash populated
- plaintext password not stored
- UUID v4 present
- default ACTIVE
- safe result does not contain passwordHash

B. Duplicate email
- case/whitespace normalized collision rejected
- unique DB safety retained

C. Credential verification success
- valid normalized email/password succeeds

D. Wrong password
- generic invalid result

E. Unknown email
- generic invalid result

F. INACTIVE account
- generic invalid result

G. LOCKED account
- generic invalid result

H. Password hash verification
- actual Argon2 hash used

33. Test Isolation
------------------
Use dedicated:
catechism_api_test

Do not use dev DB.

Tests must clean up their own rows or reset safely.

Avoid order dependence.

Do not reset whole DB per individual test if unnecessary.

34. Unit Tests
--------------
Add unit tests for:
- email normalization
- password policy
- PasswordHashService
- safe mapping
- status behavior where appropriate

Do not mock every TypeORM internal detail if integration tests cover repository behavior better.

35. Argon2 Native Module Validation
-----------------------------------
After installing argon2:

Run local:
npm install / npm ci as appropriate

Then verify:
- unit test hash/verify
- npm run build
- Docker production build

Production Docker build is mandatory for this prompt because Argon2 may include native binaries.

Use:
wsl docker build --target production -t catechism-api:auth-user .

or equivalent current Docker command.

36. CI Compatibility
--------------------
Existing Bitbucket CI Docker build gate should catch native module issues.

Audit package placement:
argon2 must be runtime dependency, not devDependency.

Do not edit pipeline unless a real argon2-specific issue requires it.

37. Logging / Secret Safety
---------------------------
Search implementation for:
password
passwordHash

Ensure:
- plaintext never logged
- hash never logged
- no accidental object serialization with password field
- errors do not include plaintext password

38. Public Contract Boundary
----------------------------
Future AuthModule should only need:

UserAccountService.verifyCredentials(...)

Potential account lookup by id for status validation may also be useful later.

If adding:
getActiveAccountById / getAccountAuthState

keep return type safe.

Do not expose repository/query builder.

39. Future Microservice Extraction Review
-----------------------------------------
Document how UserModule can later become Identity/User service.

Public service contract today should map naturally to future:
- RPC/HTTP credential verification
- account-state lookup

No caller should depend on UserEntity.

40. Module Boundary Test Update
-------------------------------
Update module-boundary test:

UsersModule may now export its public service.

Ensure it still does NOT export:
- TypeOrmModule
- UserEntity
- repository

AuthModule and AccessControlModule should remain with no exports at this stage unless current code requires otherwise.

41. No Seed/Admin Provisioning
------------------------------
Do NOT create initial admin account.

Do NOT seed password.

Admin provisioning will be designed after Auth/RBAC guards exist.

42. Required Validation
-----------------------
Run:

node --version
npm --version
npm install / npm ci as appropriate after dependency change
npm run format
npm run format:check
npm run lint
npm run typecheck
npm test
npm run test:e2e
npm run build
npm audit --audit-level=moderate
npm run quality

With MSSQL:
npm run test:db:migrations
npm run test:integration
npm run test:e2e:db
npm run quality:full

Native dependency:
production Docker build must PASS.

43. Git Policy — CRITICAL
-------------------------
Cursor MUST NEVER run:

git add
git commit
git push

If tracked changes exist, print exactly ONE recommended command:

git commit -m "feat(auth): add user account credential service"

Cursor must NOT execute it.

44. Required Report
-------------------
Create:

docs/AUTH_003_USER_ACCOUNT_PASSWORD_FOUNDATION_REPORT.md

Required sections:

1. Objective

2. State Inherited From AUTH #002B

3. Module Boundary Audit Before Implementation

4. Dependencies Added
Exact versions.

5. Argon2 Compatibility Result

6. Files Created

7. Files Modified

8. UserModule Architecture

9. Public UserModule Exports

10. Email Normalization Strategy

11. Email Validation

12. Password Policy

13. Password Hashing Strategy
- Argon2id
- parameters
- hash format

14. Account Creation Flow

15. Safe Account Contract

16. Credential Verification Contract

17. Account Status Behavior

18. Duplicate Email Handling

19. Enumeration / Timing Considerations

20. Repository Boundary

21. Unit Tests

22. Integration Tests

23. Argon2 Native Docker Validation

24. Existing CI Compatibility

25. Security / Secret Review

26. Module Boundary Audit After Implementation

27. Future Microservice Extraction Review

28. Commands Executed

29. Validation Results
Explicit PASS/FAIL:
- format
- lint
- typecheck
- unit
- DB-free e2e
- build
- audit
- quality
- migrations
- DB integration
- DB e2e
- quality:full
- Argon2 hash/verify
- production Docker build
- duplicate normalized email
- no persistence export
- no cross-module repository access

30. Known Issues / Deferred Items

31. Out-of-Scope Confirmation
Confirm no:
- login API
- JWT
- refresh sessions
- RBAC guards
- seeds
- admin provisioning

32. AUTH #004 Readiness

Recommend:
AUTH/RBAC #004 — Authentication + Access Token

It should implement:
- AuthModule service
- login endpoint
- JWT access token
- auth config
- JwtAuthGuard
- CurrentUser/Public decorators as needed
- uses UserAccountService public contract
- no refresh token lifecycle yet beyond any minimum session prerequisite decided carefully

33. Commit Message Recommendation

Print exactly ONE:

git commit -m "feat(auth): add user account credential service"

Cursor MUST NOT execute it.

45. Final Response
------------------
When finished, respond concisely with:

- AUTH/RBAC #003 status
- password/account service PASS/FAIL
- Argon2 Docker compatibility PASS/FAIL
- module boundary PASS/FAIL
- report path:
  docs/AUTH_003_USER_ACCOUNT_PASSWORD_FOUNDATION_REPORT.md

Then print exactly ONE:

git commit -m "feat(auth): add user account credential service"

Do NOT run:
git add
git commit
git push

Do NOT proceed to AUTH #004 automatically.
