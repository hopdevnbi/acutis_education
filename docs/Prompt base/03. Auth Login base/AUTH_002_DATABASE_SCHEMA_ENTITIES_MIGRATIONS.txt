CURSOR PROMPT — AUTH/RBAC #002/8
IMPLEMENT DATABASE SCHEMA + ENTITIES + MIGRATIONS ONLY

1. Context
----------
We are building a long-term parish catechism backend platform.

Completed:
- Backend Foundation: COMPLETE
- Backend Bitbucket CI Foundation: COMPLETE
- AUTH/RBAC #001 — Modular Rules + Auth/RBAC Design Audit: COMPLETE

Current phase:
Auth + User + Role + Permission

Prompt:
AUTH/RBAC #002 / 8

Planned sequence:
#001 Modular/microservice-ready rules + Auth/RBAC audit
#002 Database schema + entities + migrations
#003 User module + password/account lifecycle foundation
#004 Authentication + access token
#005 Refresh token / session architecture
#006 Role + Permission + RBAC guards
#007 Auth/RBAC integration tests + security hardening
#008 Final Auth/RBAC audit + module-boundary review

This prompt is schema/persistence only.

DO NOT implement controllers/login/JWT/guards yet.

2. Required Reading
-------------------
Before changing anything:

1. Read PROJECT_RULES.md, especially:
   - modular architecture / future microservice extraction rules
   - database/migration rules
   - security/privacy rules
2. Read AGENTS.md
3. Read all .cursor/rules/*.mdc
4. Read:
   docs/AUTH_001_MODULAR_RULES_AND_AUTH_RBAC_AUDIT_REPORT.md
5. Read:
   docs/PROMPT_007_FINAL_BACKEND_FOUNDATION_AUDIT_REPORT.md
6. Inspect:
   src/database/
   src/modules/ (if now present)
   test/database/
   package.json
   TypeORM naming strategy
   migration scripts
7. Inspect current Git status with read-only commands.

3. Primary Objective
--------------------
Implement the persistence foundation for:

UserModule
AuthModule
AccessControlModule

Create only:

- module skeletons where needed for entity ownership
- TypeORM entities
- relationships
- constraints
- indexes
- migration(s)
- schema/integration tests

Target tables:

UserModule:
- users

AccessControlModule:
- roles
- permissions
- user_roles
- role_permissions

AuthModule:
- auth_sessions

No auth behavior yet.

4. Modular Ownership Rules
--------------------------
CRITICAL:

Each table/entity must belong to exactly one module.

UserModule owns:
- users

AccessControlModule owns:
- roles
- permissions
- user_roles
- role_permissions

AuthModule owns:
- auth_sessions

Do NOT:
- place auth entities in shared/common folder
- create a giant auth entity folder
- access another module's repositories
- expose TypeORM entities as public contracts
- create cross-module service logic yet

5. Proposed Folder Structure
----------------------------
Preferred minimal structure:

src/modules/users/
  users.module.ts
  entities/
    user.entity.ts

src/modules/access-control/
  access-control.module.ts
  entities/
    role.entity.ts
    permission.entity.ts
    user-role.entity.ts
    role-permission.entity.ts

src/modules/auth/
  auth.module.ts
  entities/
    auth-session.entity.ts

Adjust only if current project conventions strongly justify another structure.

Do not add controllers/services/guards unless required just for Nest registration.

6. User Entity
--------------
Implement `users`.

Required baseline fields from AUTH #001:

id
- MSSQL uniqueidentifier
- PK
- UUID v4 generated by application/entity hook strategy consistent with project foundation

email
- nvarchar(320)
- required
- globally unique for phase 1
- stored normalized lowercase/trimmed in application layer later
- DB uniqueness still required

password_hash
- nvarchar(255)
- required
- never plaintext

status
- varchar(32)
- required
- values conceptually:
  ACTIVE
  INACTIVE
  LOCKED

created_at
- datetime2
- required

updated_at
- datetime2
- required

Do NOT add:
- profile data
- parish_id
- class_id
- student relationship
- parent relationship
- failed_login_count
- locked_at
- email_verified
- last_login_at

unless AUTH #001 explicitly marked them required now (it did not).

7. User Status Representation
-----------------------------
Decide implementation carefully.

Preferred:
a TypeScript enum local to UserModule for account status may be acceptable because this is a technical account lifecycle enum, not a data-driven RBAC role.

If TypeORM enum mapping to MSSQL is awkward/unhelpful:
store varchar and validate through TypeScript enum.

Do not create DB lookup table just for account status.

Document choice.

8. Roles Entity
---------------
Implement `roles`.

Fields:

id
- uniqueidentifier PK

code
- stable machine-readable role code
- varchar/nvarchar reasonable size
- required
- unique

name
- human-readable
- required

description
- nullable

created_at
updated_at

Do NOT hard-code role rows as a TypeScript enum.

Role rows remain data-driven.

No seeds in this prompt.

9. Permissions Entity
---------------------
Implement `permissions`.

Fields:

id
- uniqueidentifier PK

code
- stable machine-readable permission code
- lowercase convention `{resource}.{action}`
- required
- unique

name
- human-readable
- required

description
- nullable

created_at
updated_at

Do NOT create actual permission catalog rows yet.

No seed data.

10. User Roles Join Entity
--------------------------
Implement `user_roles`.

Ownership:
AccessControlModule

Fields:
- user_id
- role_id
- created_at if useful for assignment auditing

Required:
- FK user_id -> users.id
- FK role_id -> roles.id
- unique(user_id, role_id)

Choose either:
A. composite primary key
or
B. separate UUID PK + composite unique

Prefer simplest model.

For pure join tables, composite PK may be appropriate.

Document decision.

Delete behavior:
prefer RESTRICT / NO ACTION over cascading user/role deletion unless strongly justified.

11. Role Permissions Join Entity
--------------------------------
Implement `role_permissions`.

Fields:
- role_id
- permission_id
- created_at if useful

Required:
- FK role_id -> roles.id
- FK permission_id -> permissions.id
- unique(role_id, permission_id)

Same PK strategy philosophy as user_roles.

Prefer non-destructive FK behavior.

12. Auth Session Entity
-----------------------
Implement `auth_sessions`.

Owned by AuthModule.

Fields:

id
- uniqueidentifier PK
- session id

user_id
- uniqueidentifier
- FK -> users.id
- indexed

refresh_token_hash
- nvarchar(255)
- required

token_family_id
- uniqueidentifier
- required
- indexed

expires_at
- datetime2
- required

revoked_at
- datetime2 nullable

created_at
- datetime2 required

Do NOT store:
- raw refresh token
- IP
- user agent
- device fingerprint

13. Refresh Token Hash Length
-----------------------------
Audit hash representation.

AUTH #001 currently proposes nvarchar(255).

Ensure chosen future hash/HMAC format fits.

Do not over-expand without reason.

Document chosen size.

14. Cross-Module Foreign Keys
-----------------------------
Allowed:
- auth_sessions.user_id -> users.id
- user_roles.user_id -> users.id

This is acceptable inside modular monolith for integrity.

But:
- application ownership remains separate
- AuthModule will not manipulate users repository
- AccessControlModule will not manipulate users table directly

Document this explicitly.

15. Entity Relationships
------------------------
Be careful with TypeORM object relationships.

Avoid creating bidirectional entity relationships everywhere if they create cross-module entity imports and tight coupling.

This is IMPORTANT for future microservice extraction.

Preferred:
- use explicit FK scalar columns across module boundaries
- avoid importing UserEntity into AuthSessionEntity merely for convenience if not necessary
- use database migration FK constraints independently

Within AccessControlModule, entity relationships may be used if clean.

Evaluate and choose intentionally.

Priority:
module boundary > ORM convenience.

16. No Cross-Module Entity Imports
----------------------------------
Strong preference:

AuthSessionEntity should have:
userId: string

not:
@ManyToOne(() => UserEntity)

if that would require AuthModule entity code importing UserModule persistence entity.

Similarly UserRoleEntity should not need to import UserEntity if FK can be represented safely via scalar + migration constraint.

If TypeORM requires relation metadata for some use case later, defer it.

The database migration can enforce FK.

17. Naming Conventions
----------------------
Follow existing snake_case strategy.

Expected physical names:
users
roles
permissions
user_roles
role_permissions
auth_sessions

Columns:
password_hash
user_id
role_id
permission_id
token_family_id
refresh_token_hash
expires_at
revoked_at
created_at
updated_at

Do not manually fight naming strategy unless needed for exact constraint names.

18. Index / Constraint Naming
-----------------------------
Prefer explicit deterministic names for important constraints where useful.

Examples conceptually:
UQ_users_email
UQ_roles_code
UQ_permissions_code
UQ_user_roles_user_role
UQ_role_permissions_role_permission
IX_auth_sessions_user_id
IX_auth_sessions_token_family_id

Use naming conventions consistent with MSSQL/project style.

Do not over-name every default constraint if TypeORM handles it cleanly.

19. Email Uniqueness
--------------------
Global email uniqueness for phase 1.

Audit MSSQL collation behavior.

Application layer later will normalize:
trim + lowercase

DB unique constraint is still required.

Do NOT implement computed normalized email column unless necessary.

Keep phase 1 simple.

20. Timestamps
--------------
Use UTC semantics.

Choose TypeORM timestamp configuration consistent across entities.

Prefer:
datetime2

Do not rely on database local timezone.

Document whether timestamps are application-generated or DB defaulted.

Consistency matters more than mechanism.

21. UUID Strategy
-----------------
Foundation decision:
application-generated UUID v4
MSSQL uniqueidentifier

Keep this decision.

Use:
crypto.randomUUID()
or existing project helper/pattern.

Do not switch to NEWID() DB defaults unless justified.

22. Migration Strategy
----------------------
Create explicit migration(s).

Do NOT use synchronize.

Possible:
one migration for whole AUTH/RBAC schema

or
separate migrations per bounded module.

Given future extraction and ownership, evaluate preferably:

- users migration
- access-control migration
- auth-sessions migration

BUT avoid unnecessary migration fragmentation if dependencies complicate ordering.

A good compromise may be 3 migrations:
1. users
2. access control
3. auth sessions

Respect FK dependency ordering.

Document decision.

23. Migration Ownership
-----------------------
Current project uses central:
src/database/migrations/

This is acceptable for TypeORM runtime.

But migration names should clearly reflect module ownership.

Examples:
CreateUsersTable
CreateAccessControlTables
CreateAuthSessionsTable

Do not move migration infrastructure into feature folders unless current architecture supports it cleanly.

24. Migration Revert
--------------------
Every migration must implement safe `down`.

Drop order must respect FK dependencies.

Never:
- disable all constraints globally
- drop unrelated tables

25. Migration Testing
---------------------
Run migration on:
- dedicated test DB
- development DB only if safe and explicitly appropriate

Preferred:
test DB first.

Validate:
- tables exist
- columns correct
- FKs exist
- indexes/uniques exist
- no extra tables except migration metadata

Then run integration tests.

26. Fresh Database Validation
-----------------------------
Use test DB reset mechanism safely.

Preferred:
npm run test:db:prepare -- --reset

ONLY against `catechism_api_test`.

Guard must reject dev DB.

Then run migrations.

Verify clean install from zero.

This is critical.

27. Migration Idempotency
-------------------------
TypeORM migrations themselves run once via migration metadata.

Do not write manual IF EXISTS/IF NOT EXISTS unless needed.

Validate:
- first run applies
- second run reports no pending migrations

28. Schema Verification Tests
-----------------------------
Add integration tests that query MSSQL metadata.

At minimum verify:

users:
- exists
- email unique
- password_hash exists
- status exists

roles:
- exists
- code unique

permissions:
- exists
- code unique

user_roles:
- FK user
- FK role
- unique pair

role_permissions:
- FK role
- FK permission
- unique pair

auth_sessions:
- FK user
- index user_id
- index token_family_id
- refresh_token_hash exists
- expires_at
- revoked_at nullable

No data required.

29. Entity Metadata Tests
------------------------
Add focused unit tests if useful for:
- table names
- column types
- no cross-module ORM relations/imports

Do not create brittle tests against every decorator detail unless valuable.

30. Module Skeletons
--------------------
Create minimal:

UsersModule
AccessControlModule
AuthModule

At this prompt, they may simply register TypeORM entities required for future services.

Do not export repositories.

Do not add service logic yet.

Be careful:
AuthModule may later depend on UsersModule/AccessControlModule, but do not add those imports prematurely if no service needs them yet.

31. AppModule Registration
--------------------------
Decide whether to register these modules now.

Preferred:
Yes, if entity discovery/runtime expects feature modules to be part of AppModule.

But ensure:
- no controllers/endpoints
- no side effects
- app bootstrap remains healthy

Document decision.

32. TypeORM autoLoadEntities
----------------------------
Current database architecture uses autoLoadEntities.

Register entities via:
TypeOrmModule.forFeature([...])

within owning modules.

Do not globally list all business entities in DatabaseModule.

This reinforces ownership.

33. No Repositories Yet
-----------------------
Do NOT create repository classes unless needed for schema tests.

Persistence behavior comes in later prompts.

Entities + module registration are enough.

34. Argon2 Compatibility Check
------------------------------
AUTH #001 selected Argon2id for AUTH #003 but explicitly asked AUTH #002 to verify compatibility.

Do NOT implement password hashing yet.

But perform a compatibility audit/experiment:

- verify current Node 22.23.1 environment
- verify production Docker base
- verify CI image compatibility

You may install `argon2` only if needed to prove build compatibility AND if we want to lock the dependency now.

Preferred:
do not add hashing dependency in schema prompt unless necessary.

Alternative:
use `npm view`/temporary install test if available.

However, if you install `argon2` into project dependencies, that is scope creep unless justified.

Best approach:
audit package compatibility and, if practical, test in a temporary isolated manner without changing package.json.

Document:
- ready for AUTH #003
or
- fallback recommendation bcrypt

Do not browse externally if local package metadata/tooling is sufficient; if current compatibility is uncertain, use available package manager information.

35. No Seed Data
----------------
Do NOT seed:
- SUPER_ADMIN
- PARISH_ADMIN
- CATECHIST
- PARENT
- permissions

Seed strategy will come after RBAC implementation.

36. No API
----------
Do NOT create:
- /auth/login
- /auth/refresh
- /auth/logout
- /auth/me
- /users
- /roles
- /permissions

No controllers.

37. No Security Packages
------------------------
Do NOT add:
- @nestjs/jwt
- passport
- passport-jwt
- argon2
- bcrypt

unless Argon2 compatibility validation absolutely requires installing and keeping it, which is not preferred.

38. Tests Must Preserve Existing Layers
---------------------------------------
Keep:
npm test
npm run test:e2e
npm run test:integration
npm run test:e2e:db
npm run quality
npm run quality:full

New schema tests should fit existing integration test architecture.

Do not make DB-free suites require MSSQL.

39. CI Compatibility
--------------------
Any new entities/migrations/tests must pass existing CI gates.

Run:
npm run quality

and with MSSQL:
npm run quality:full

Also verify production Docker build if entity imports affect build.

40. Module Boundary Audit
-------------------------
Because PROJECT_RULES now requires it, include explicit boundary review:

UserModule:
- owns users
- exports no persistence entity/repository

AccessControlModule:
- owns RBAC tables
- no UserRepository access

AuthModule:
- owns sessions
- no UserEntity import if scalar FK strategy chosen

Check dependency graph after implementation.

No cycles.

41. Future Microservice Extraction Review
-----------------------------------------
After schema implementation, document:

User service extraction:
- users table

Auth service extraction:
- auth_sessions
- replace FK with external user-id validation later

Policy service extraction:
- roles/permissions/joins
- user_roles would need user-id reference without SQL FK later

Ensure current schema does not create application-level coupling that prevents this.

42. Security Review
-------------------
Verify:
- password_hash column only, no plaintext
- refresh_token_hash only, no raw token
- no IP/user-agent fields
- no default admin password
- no secret values
- no public endpoints
- no logging of auth data

43. Required Validation
-----------------------
Run:

node --version
npm --version
npm run format
npm run format:check
npm run lint
npm run typecheck
npm test
npm run test:e2e
npm run build
npm audit --audit-level=moderate

With MSSQL available:

npm run test:db:prepare -- --reset
npm run test:db:migrations
npm run test:integration
npm run test:e2e:db
npm run quality:full

Validate:
migration show/run/re-run semantics.

Production Docker build:
docker build --target production -t catechism-api:auth-schema .

Use WSL docker equivalent if local environment requires it.

44. Git Policy — CRITICAL
------------------------
Cursor MUST NEVER run:

git add
git commit
git push

If tracked changes exist:
print exactly ONE recommended command:

git commit -m "feat: add auth and access control schema"

Cursor must NOT execute it.

45. Required Report
-------------------
Create:

docs/AUTH_002_DATABASE_SCHEMA_ENTITIES_MIGRATIONS_REPORT.md

Required sections:

1. Objective

2. State Inherited From AUTH #001

3. Module Boundary Implementation

4. Files Created

5. Files Modified

6. Users Schema

7. Roles Schema

8. Permissions Schema

9. User Roles Schema

10. Role Permissions Schema

11. Auth Sessions Schema

12. Entity Design Decisions

13. Cross-Module FK Strategy

14. Cross-Module Entity Import Audit

15. UUID Strategy

16. Timestamp Strategy

17. Index / Constraint Design

18. Migration Structure
List migration order and ownership.

19. Migration Up/Down Review

20. Fresh Test DB Validation

21. Migration Re-run Validation

22. Schema Metadata Tests

23. Existing Test Regression

24. Module Registration

25. Argon2 Compatibility Audit
No hashing implementation yet.

26. Security Review

27. Modular / Microservice Extraction Review

28. Commands Executed

29. Validation Results
Explicit PASS/FAIL:
- format
- lint
- typecheck
- unit
- DB-free e2e
- build
- audit
- test DB reset
- migrations
- schema tests
- integration
- DB e2e
- quality:full
- Docker production build

30. Git Status / Scope Summary

31. Known Issues / Assumptions

32. Out-of-Scope Confirmation
Confirm no:
- login
- JWT
- refresh API
- guards
- role/permission business logic
- seeds
- admin provisioning

33. Prompt #003 Recommendation

AUTH/RBAC #003 — User Module + Password/Account Lifecycle Foundation

It should implement:
- UsersService
- credential verification boundary
- email normalization
- password hashing with Argon2id if compatibility confirmed
- account status checks
- account creation/service-layer tests as needed
- NO JWT/login controller yet

34. Commit Message Recommendation

Print exactly ONE:

git commit -m "feat: add auth and access control schema"

Cursor MUST NOT execute it.

46. Final Response
------------------
When finished, respond concisely with:

- AUTH/RBAC #002 status
- schema/migrations PASS/FAIL
- module boundary PASS/FAIL
- report path:
  docs/AUTH_002_DATABASE_SCHEMA_ENTITIES_MIGRATIONS_REPORT.md

Then print exactly ONE:

git commit -m "feat: add auth and access control schema"

Cursor MUST NOT run:
git add
git commit
git push

Do NOT proceed to AUTH #003 automatically.
